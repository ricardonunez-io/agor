# Native CLI Feature Gaps

**Status:** ✅ Superseded

The detailed comparison between native agent CLIs (Claude Code, Gemini CLI, Cursor, etc.) and SDK integrations now lives in the public guide:

👉 https://agor.live/guide/sdk-comparison

That page is kept up to date alongside each release, so treat it as the canonical source instead of duplicating the matrix in this repo.
